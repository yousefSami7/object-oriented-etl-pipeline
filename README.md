# Object-Oriented ETL Pipeline
This program extracts data from a live internet API and a local CSV file. It cleans the data, merges it together, and saves a final inventory report.

## Setup Instructions
To install the required dependencies for this pipeline, open your terminal and run:
pip install -r requirements.txt

## How to Run
To execute the data extraction and generate the final inventory report, run:
python main.py
